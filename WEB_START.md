# 웹 실행

HTML 버전은 `README.md`의 실행 순서를 따라 주세요. `start.command` 또는 FastAPI로 실행합니다.

기존 Streamlit 화면도 `crypto_xgb_web.py`로 보존했습니다. 선택적으로 `python -m pip install -r requirements_web.txt` 후 `python -m streamlit run crypto_xgb_web.py`로 실행합니다. Streamlit 화면은 이번 HTML 검증 범위에 포함하지 않았습니다.
