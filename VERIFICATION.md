# 검증 결과 — 2026-09-07

## 실행 환경

- macOS ARM64, Python 3.12.14
- 별도 가상환경에 requirements.txt 설치 성공
- pandas 2.3.1, numpy 2.2.6, scikit-learn 1.7.1, XGBoost 3.0.4
- FastAPI 0.116.1, uvicorn 0.35.0
- 원본의 data/와 모델에 쓰지 않고 임시 폴더에서 새로 수집·학습

## 실제 데이터 검증

Binance 공개 선물 API HTTP 200을 확인했습니다.
BTC·ETH·SOL·XRP × 15m·1h·4h·1d의 16개 조합에서 최근 캔들 수신을 확인했습니다.

BTC에 대해서는 새 CSV 생성부터 학습, 방향 확률 및 Q10/Q50/Q90 예측까지 실제 실행했습니다.

| 시간봉 | 종료 캔들 | 방향 예측 | 가격 구간 예측 |
|---|---:|---|---|
| 15분 | 35,040 | 성공 | 성공 |
| 1시간 | 26,280 | 성공 | 성공 |
| 4시간 | 10,950 | 성공 | 성공 |
| 1일 | 2,555 | 성공 | 성공 |

BTC 4시간봉 재실행은 두 모델을 재사용했고 약 0.52초 걸렸습니다.
이는 해당 컴퓨터의 단일 측정값이며, 첫 실행·다른 서버의 소요 시간을 보장하지 않습니다.

## 자동 검증

- Python unittest 25개 통과
- 기존 피처 타깃 정렬, 미래 데이터 누수 방지, Lockbox 분리, 수수료 정산 테스트 포함
- 시간 초과 최대 재시도, 영구 제한 재시도 금지, 실패 시 CSV 보존, 체크포인트 중복 제거
- 미완성 캔들 제외, 결측·무한대·잘못된 고저가 거절
- 저장 데이터 읽기가 다운로드 잠금을 기다리지 않는지 확인
- 잘못된 입력 422, 미존재 차트 404, 중복 작업 409, 최종 검증 사전 확인
- 방향 예측 성공 후 구간 계산 실패 시 부분 결과 보존
- 경로·데이터 클래스·NaN JSON 정리
- 기본 차트 2,000봉 및 전체 차트 응답, 전체 이력 기반 EMA 유지
- JavaScript 누락값 표시, 구간 결과 누락, API 오류, file:// 직접 실행 오류 테스트 통과
- Python 컴파일, JavaScript 문법, 실행 셸 문법 검사 통과

## 범위와 한계

원본 CCXT의 시장 목록 조회도 이 컴퓨터에서는 성공했습니다.
따라서 사용자가 기존 서버에서 경험한 오류를 동일하게 재현했다고 주장하지 않습니다.
코드에서 확인한 실패 처리·데이터 요청·화면 상태 문제를 수정하고 실제 수신과 예측을 검증했습니다.

다른 거래소 데이터·현물·테스트넷으로 몰래 대체하지 않습니다.
네트워크 오류는 기존 데이터를 보존하되 최신 예측 성공으로 위장하지 않습니다.

전체 Walk-Forward를 새로 실행하거나 최종 Lockbox를 소모하지 않았습니다.
기존 테스트로 해당 핵심 계산을 확인했으며, 이번 수정으로 예측 정확도가 좋아졌다는 검증은 하지 않았습니다.
브라우저 시각·클릭 자동화와 보존한 Streamlit 화면은 검증 범위에 포함하지 않았습니다.
로컬 HTML 경로 응답 및 실제 API를 확인했고 미리보기를 열었습니다.

공개 배포·다중 서버 운영·장기 부하·다른 서버 지역의 API 접근은 검증하지 않았습니다.
작업 상태는 단일 프로세스에 있으므로 worker=1로 운영해야 합니다.
